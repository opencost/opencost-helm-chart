{{- if .Values.saml }}
{{- if .Values.saml.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "costmodel.fullname" . }}-saml
  labels:
    {{ include "costmodel.commonLabels" . | nindent 4 }}
data:
{{- $root := . }}
    saml.json: '{{ toJson .Values.saml }}'
{{- end -}}
{{- end -}}